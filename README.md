# Open-World LLM Agent

An agent that can:
1.  Accept user tasks.
2.  Detect environment (OS, tools).
3.  Generate plans using LLM + procedural memory.
4.  Execute safely.
5.  Observe and learn.

## Usage

```bash
poetry install
poetry run python src/main.py "Your task here"
```
